# NightWatch — All-in-one Render requirements (standalone, no -r)
# Use this instead of requirements.txt if -r doesn't resolve correctly on Render

click>=8.1.0
aiohttp>=3.9.0
dnspython>=2.4.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
jinja2>=3.1.0
rich>=13.7.0
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
flask>=3.0.0
gunicorn>=21.0.0
